# Vision fixtures

These redistributable synthetic fixtures contain no customer data. `equipment.png`
is a valid deterministic PNG generated from geometric shapes and labels. The
opt-in modality test decodes it with Pillow and the revision-pinned local SmolVLM
backend; it is evidence of execution and provenance, not semantic accuracy.
